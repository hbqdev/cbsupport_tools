# Backup Skills

Diagnostic references for Couchbase backup and restore issues — cbbackupmgr, the Backup Service, and CAO-managed `CouchbaseBackup` resources.

| Skill | When to read |
| --- | --- |
| [cbbackupmgr-failures.md](cbbackupmgr-failures.md) | A backup fails outright or runs far longer than expected: `database or disk is full` / staging exhaustion, `DCP stream closed unexpectedly`, `Stream closed by backup client`, DCP `BufferLogFull` pauses, or "why is our smaller cluster slower to back up?" |
